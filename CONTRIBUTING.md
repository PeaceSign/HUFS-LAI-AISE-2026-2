# 기여하는 법

이 문서의 규칙은 대부분 실제 오픈소스 프로젝트에서 쓰는 것 그대로입니다. 여기서 익힌 게 밖에서도 통합니다.

## 브랜치

```
<학번>/<짧은-설명>
```

예: `202512345/add-member`, `202512345/fix-empty-input`

`main` 에 직접 push 하지 않습니다.

## 커밋

한 커밋에 한 가지 변경. "여러 개를 한꺼번에" 는 리뷰가 불가능해집니다.

메시지 첫 줄은 **무엇을 했는지**를 명령형으로 적습니다.

```
members에 202512345 추가
빈 입력에서 나던 IndexError 수정
```

## PR 제목

```
<type>: <설명>
```

| type | 언제 |
|---|---|
| `feat` | 기능 추가 |
| `fix` | 버그 수정 |
| `docs` | 문서 |
| `test` | 테스트 추가·수정 |
| `refactor` | 동작은 그대로, 구조만 변경 |
| `chore` | 설정·의존성 등 |
| `ci` | 자동화 |

예: `docs: members에 202512345 추가`

자동 검사가 돕니다. 형식이 틀리면 봇이 규칙을 댓글로 남기고 검사가 실패합니다. 제목을 고치면 다시 돕니다.

## PR 크기

**300줄을 넘기지 않습니다.** 리뷰에서 잡히는 결함은 변경이 커질수록 급격히 줄어듭니다. 크면 나눠서 올립니다.

## 리뷰

- 병합하려면 **approval 1개**가 필요합니다. 동료 리뷰가 먼저입니다
- 동료 approval 이 붙으면 교수가 자동으로 리뷰어에 추가됩니다
- 하루 이상 리뷰가 안 붙은 PR 에는 봇이 리마인더를 남깁니다

리뷰할 때는 고칠 곳만 말하지 말고 **왜 그렇게 생각하는지**를 적습니다. 리뷰는 상대를 검사하는 게 아니라 둘이 같이 코드를 보는 일입니다.

## 이슈

작업을 시작하기 전에 이슈를 엽니다. 이슈에는 **완료 조건**을 적습니다 — 무엇이 되면 닫을 수 있는지가 없으면 끝을 알 수 없습니다.

처음 기여한다면 `good first issue` 라벨이 붙은 것부터 봅니다.
